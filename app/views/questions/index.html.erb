<div class="content">

  <h3><%= link_to "Questions", questions_path %></h3>
  <%= render 'new' %>

  <table class="table table-striped">
    <thead>
      <tr>
        <th><%= sort_link @q, :user_id, "User" %></th>
        <th><%= sort_link @q, :title, "Title" %></th>
        <th><%= sort_link @q, :body, "Body"%></th>
        <th><%= sort_link @q, :tags, "Tags"%></th>
        <th><%= sort_link @q, :created_at, "Date" %></th>
      </tr>
    </thead>
    <tbody>
      <% @questions.reverse_each do |question| %>
      <tr>
        <td class="text-center"><a href="/users/<%= question.user_id %>"><%= image_tag question.user.avatar.url(:thumb), class: "thumb-index" %>
        <%= question.user.username %></a></td>
        <td><h4><%= link_to question.title, question_path(question) %></h4></td>
        <td><%= question.body.html_safe %></td>
        <td><i class="fa fa-tag" aria-hidden="true"></i> <%= link_to question.tags.first, tag_path(question.tags.first) %></td>
        <td><%= question.created_at.localtime.strftime("%B %d, %Y") %></td>
        <% end %>
      </tr>
    </tbody>
  </table>

</div>

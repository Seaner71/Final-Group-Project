<h1>Questions#index</h1>
<p>Find me in app/views/questions/index.html.erb</p>

<%= link_to 'Ask a question', new_question_path %>

<% @questions.last(10).reverse_each do |question| %>
  <div class="question-box">
        <a href="questions/<%= question.id %>"> <%= question.title %></a>
      Posted on: <%= question.created_at.localtime.strftime("%B %d, %Y %I:%M:%S %p") %>
    <%= link_to question.tag1, tags_index_path %>
  <%= link_to "Edit Question", edit_question_path(question) %>
    <div class="comment-box">
        <strong>Answers</strong><br>
        <%= 'No answer yet' if question.answers.empty? %>

        <% question.answers.last(3).reverse.each do |answer| %>
        <li><%=answer.content %> </li>

        <% end %>

    </div>
 </div>


<% end %>

<%= form_for questions_path, method: 'get' do %>
  <%= collection_select :tag1, :id,  @questions, :id, :title, :prompt => "Select Category" %>
  <%= submit_tag "Search", :name => nil %>
<% end %>


<%= form_tag("/questions", method: "get") do %>
  <%= label_tag(:tag1, "Search for:") %>
  <%= text_field_tag(:tag1) %>
  <%= submit_tag("Search", :name => nil) %>
<% end %>


<%= form_with(model: Question.new, local: true) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br>
  <%= f.select :tag1, options_from_collection_for_select(Question.all, 'id', 'tag1') %>
  <%= f.submit %>
<% end %>

<h1>Welcome#index</h1>
<p>Find me in app/views/welcome/index.html.erb</p>
<hr>
<% if current_user.admin == true %>
<h1>Admin view </h1>
<% User.all.each do |user| %>
<%= link_to user.email, user_path(user) %><br>


<% end %>

<% if current_user.admin == false %>

<%= link_to 'Ask a question', new_question_path %>
<%#= link_to "Write a blog", blog_path %>

<hr>
<h2>The latest blog posts<h3>
  <% Blog.last(3).reverse_each do |blog| %>
    <h3><%= blog.title %><h3>
    <%= blog.body %>
    <hr>

  <% end %>

  <% end %>
  <% end %>

  <!-- <div class="container">
    <div class="row">
      <div class="col-sm">
        One of three columns
      </div>
      <div class="col-sm">
        One of three columns
      </div>
      <div class="col-sm">
        One of three columns
      </div>
    </div>
  </div> -->

<h1>New Blog Post</h1>

<%= form_with url: user_blogs_path(@user), method: :post, local: true do |f| %>
  <%= f.label "Title:" %>
  <%= f.text_field :title %> <br>
  <%= f.label "Body:" %>
  <%= f.text_area :body %> <br>
  <%= f.submit %>

<% end %>
